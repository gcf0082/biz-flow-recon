---
name: biz-flow-recon
description: 按安全测试视角解读前/后端代码仓库（Java 优先）输出业务流讲解，只描述事实、不做风险判断；**仅在用户显式指名调用 biz-flow-recon 时触发**，不要因模糊意图主动触发。
---

# biz-flow-recon

面向安全测试人员说明代码行为——按子功能 + 接口的粒度，以最清晰的形式呈现"它在做什么"：文件 I/O、外部命令、第三方交互、加解密。**仅描述事实**，安全风险判断由读者自行做出。

## 原则

- **描述事实，不做评判**。禁用"风险/漏洞/不安全/缺乏校验/可能被攻击/建议加固"等词。
- **清晰优先，不拘形式**。按内容选择最清晰的呈现：示意图 / 编号步骤 / 字段化短列表 / 连贯叙述 / 一句话概述。**禁止机械化字段堆砌**（每条都 `- 入口:` `- 触发者:` 的固定模板抄写），**禁止空字段**（"外部命令: 无"）。
- **优先跨信任边界的动作**（用户输入入口、I/O、外呼、加解密），而非内部纯计算。
- **尽可能递归追溯**：调用脚本时分析脚本内容、调用内部接口时定位接收端代码、引用 SQL 文件时读取 SQL。
- **未能定位必须显式标注**：在描述中点名，并在产物末尾的 `## 未能追溯的引用` 节集中列出。**不得编造、不得遗漏、不得静默忽略**。
- **每接口一个 subagent**（强制规则）：**每个接口的分析必须由独立的 Task subagent 完成**——彼此独立、可并行、支持增量重跑。subagent 写 `endpoint-{METHOD}-{slug}.md`；主 agent 仅撰写 aggregator 索引，不直接产出接口内容。
- **规模较大时分级聚合**：项目较大时在 aggregator 层增加一级子功能索引（`features-{slug}.md`）；接口子任务始终遵循"每接口一个 subagent"。
- **复杂流程辅以示意图**：多服务 / 多分支 / 异步 / 状态流转时，在描述末尾追加一张 mermaid 图。
- **完整捕获请求**：REST/RPC/MQ 等结构化输入必须交代请求维度——path 变量 / query / 关键 header（鉴权、Content-Type）/ body。body 为 JSON 时给出字段树（记号见步骤 4）；DTO 类型须递归追溯到定义读出真实字段，禁止臆测。
- **追踪入参流向**：跟踪每个请求参数（path/query/header/body 字段，含 DTO 嵌套字段）是否流入下列**敏感操作位置**——拼接到路径、拼接到命令、拼接到 URL、写入文件（非日志）、拼接到 SQL。**有则使用 `**输入流向**` 一行单独标注**，按"参数 → 操作位置（文件:行号）—— 类别"格式书写。**仅描述事实（"X 拼接到 Y"），不下风险结论**。**写日志（log.info/log.error 等）一律忽略**——使用过于普遍，信噪比低。
- **细化到具体动作与目标**：文件 / 命令 / 网络操作必须给出具体**动作**（读/写/追加/删除/拷贝/exec/POST/...）与具体**目标**（完整路径 / 命令行 / URL，含 query 与关键 header）；动态拼接部分以 `/x/{var}` 模板表达并标明变量来源。**禁止**"读配置"、"调网关"、"执行脚本"等抽象表述。
- **捕获关键控制点**：流程中明示**会改变信任边界 / 跨边界行为**的**开关 / 配置 / 判断 / 限制**——TLS 与主机名校验、签名/HMAC/Token 校验启用与跳过条件、反序列化多态与类白名单、CORS 通配 + credentials、SQL `${}` 拼接（vs `#{}` 绑定）、鉴权过滤器放行规则、IP/Header 限制、上传校验（后缀/MIME/大小）、redirect 白名单、加解密算法与密钥来源（环境变量/硬编码/KMS）、token/密码强度与过期、反射/动态加载白名单等——必须以 `**关键控制点**` 标签集中列出，按 `<事项>: <实际取值或规则>（文件:行号）` 写，必要时附 `（开关）/（配置）/（判断）/（限制）` 子类。**仅描述事实（"X = 关闭" / "白名单 = [.pdf, .docx]"），不下风险结论**。普通业务功能开关（feature flag）、日志/缓存/线程池等运维配置不列。
- **不中断、不询问**：skill 一旦被显式调用即全自动跑到底——不论项目规模、未能追溯的引用数量、解析歧义。拆分决策按内置启发式（按子功能划分）直接执行；歧义按"代码事实最直接的解释"处理；错误按"显式标注 + 继续"处理（如 mermaid 渲染失败标注"渲染失败请人工检查"）。**绝不暂停等待用户输入**。用户对拆分或结果不满意可在产物落盘后调整 `_plan.md` / 删除对应 `endpoint-*.md` 重跑。

## 取舍

**保留**：用户输入入口、文件读写、外部进程、网络外呼、加解密/签名、鉴权与会话、数据持久化。
**剔除**：CSS/i18n/纯计算/单测/构建脚本/健康检查/静态资源/Spring Actuator 等纯运维端点。

## 方法

### 0. 加载工作目录

在 `<cwd>/biz-flow-recon/` 下查找：若存在 `knowledge/` 则**完整读取其中所有 .md 文件**作为先验；模板路径优先使用 `<cwd>/biz-flow-recon/templates/{name}.md`，回退至 skill 包内默认。`output/` 仅在产物落盘时再行创建。

### 1. 确定粒度

- **粒度 B（默认）**：子功能 + 接口。模板 `default.md`，使用完整骨架。
- **粒度 A**：用户明确提出"整项目/全量梳理/系统全貌"时。模板 `overview.md`。
- **粒度 C**：用户给出具体 URL 或 类#方法 时。模板仍为 `default.md`，但**仅截取一个接口块**（将 `#### POST /api/...` 提升至顶层 `# POST /api/... — 一句话功能`），不保留顶层叙述与子功能分组。

### 2. 派发 `planner` 子代理 — 枚举入口、写 _plan.md

派发 `planner`（见 `agents/planner.md`）扫全仓识别所有候选接口入口（`@RestController`/`@Controller`/`@RequestMapping`/`@Scheduled`/MQ 监听器/CLI `main`/前端 router），按目录/包/模块初步划分子功能，写出 `output/_plan.md`。**已存在 `_plan.md` 时跳过**（增量重跑）。等待其完成后进入步骤 3。

### 3. 并行派发 `interface-catalog` + N × `endpoint-analyst`

读 `_plan.md` 中的接口清单，使用 runtime 的 task / 子代理派发工具**同时启动**：

- **1 个 `interface-catalog`**——扫全仓汇总系统对外暴露接口，输出 `output/interfaces.md`
- **N 个 `endpoint-analyst`**——`_plan.md` 列出的每个接口对应一个独立子代理，传入：
  - 单接口 scope（`类#方法`、文件:行号、起始包范围）
  - 输出文件路径 `<cwd>/biz-flow-recon/output/endpoint-{METHOD}-{path-slug}.md`
  - 粒度参数（A/B/C）
  - knowledge / templates 路径

**默认并行**派发；`conventions.md` 配置 `执行模式: 串行` 时改串行。已存在的产物（`endpoint-*.md` / `interfaces.md`）默认跳过。

### 4. 等待全部完成 + 失败重试

子代理失败主 agent 重派一次（最多 2 次重试）；仍失败则在产物头部插入 `<!-- ⚠ 产物自检未通过：缺失 <具体项> 请人工补全 -->` 注释，**不阻断、不询问**，遵守"不中断、不询问"原则。

### 5. 派发 `aggregator-writer` 子代理

传入粒度参数（步骤 1 决定）。该子代理读 `output/` 下所有 `endpoint-*.md` + `interfaces.md`，按粒度撰写 aggregator 索引（`features.md` / `features-{slug}.md` / `overview.md`）；顶部加 `[对外接口清单](interfaces.md)` 链接；末尾合并所有 `## 未能追溯的引用` 节。

### 6. 派发 `completion-verifier` 子代理 — 任务收尾审计

aggregator 写完后派发 `completion-verifier`，独立审视 `output/` 全貌——核对 `_plan.md` vs 实际产出、aggregator 引用悬空 / 孤儿、各 `endpoint-*.md` 是否含 ⚠ 警告、`未能追溯的引用` 汇总一致性等，写出 `output/_audit.md`。

审计**不阻断流程**——独立 context 复核避免主 agent 自我盖章盲区，仅作为用户人工补全的依据。

### 7. 告知用户产物路径

主 agent 唯一直接面向用户的输出：列出 aggregator 入口文件（按粒度选 `overview.md` / `features.md` / `endpoint-*.md`）+ `_audit.md` 路径。**主 agent 不写任何内容文件，全程仅调度**。

---

## 子代理执行规范（endpoint-analyst 适用）

以下为 `endpoint-analyst` 子代理在分析单接口时遵守的规范。每个子代理通过自身 frontmatter prompt 引用本节为执行剧本。

每个子代理收到单接口 scope，独立执行：

追踪调用链至底层（Service/Mapper/配置/I/O/进程/网络/加密点）。遇到指向更深层代码的引用即予以追溯——常见目标：进程脚本、SQL 文件、内部 HTTP/RPC、消息 topic、反射类、动态加载的规则文件。

- **边界**：当前仓 / 本地兄弟目录 → 必须追溯；跨仓 → 仅当 `knowledge/conventions.md` 指明位置时追溯；真正外部（第三方/SaaS）→ 仅描述对外协议。
- **深度**：默认 2 层；第 3 层及以后以一句话概括。`conventions.md` 配置深度时按用户配置。
- **未能追踪到目标**：在描述中显式标注"未在工作区找到 X"，并加入文件末尾"未能追溯的引用"节。

按 `default.md` 单接口块格式撰写产物（顶层 `# {METHOD} {URL} — 一句话功能` 标题 + 描述/示意图/字段化列表 + 末尾未能追溯的引用节）。每条接口必须交代：

**具体性要求**（"细化到具体动作与目标"原则的落地）：
- **文件 I/O**：动作（读/写/追加/删除/拷贝/移动/列目录）+ 完整路径（动态部分以 `{var}` 标明变量来源）+ 格式（YAML/JSON/CSV/二进制/Logback 行日志等）
- **命令**：完整命令行（动态参数以 `{var}` 标明变量来源）+ 执行方式（`Runtime.exec`/`ProcessBuilder`）+ `文件:行号`
- **第三方**：`METHOD URL`（动态部分模板化）+ Content-Type + 关键 Header（鉴权、签名）+ body 概要 + 客户端 + `文件:行号`；复杂调用使用块状形式
- **关键控制点**：开关 / 配置 / 判断 / 限制各成一行，按 `<事项>: <实际取值或规则>（文件:行号）` 写；可选附 `（开关）/（配置）/（判断）/（限制）` 子类标签。仅描述事实，不评判风险。

> 同一接口含多个文件 / 命令 / 第三方操作时，**优先放入 flowchart 节点**呈现其执行顺序、循环、分支与 gate；labeled 列表仅保留**图无法承载的细节**（格式说明、字段含义、批量操作汇总等），**不重复图中已表达的事实**。禁止把可放入图中的多条文件/命令/外呼操作扁平堆放成 bullet 列表。

呈现形式按流程特点选择：

- **mermaid 示意图**（**仅使用 `flowchart TD`**）：所有示意图统一使用 `flowchart TD`——可承载文件/命令/外呼操作节点、关键控制点判断（`{...}` 菱形）、循环（subgraph）、分支（边标）。**禁止使用 `sequenceDiagram` / `stateDiagram-v2` / `graph LR` 等其他类型**——避免风格不一与已知渲染陷阱。
  - **图与业务描述并存**：图**前必有**自然语言说明本接口的业务目的、触发者、关键流程；图仅作增量补充，不可替代业务描述。仅画图、缺业务背景的产物视为不合格。
  - **业务尽可能融入图内**：节点标签可同时承载**业务步骤名 + 具体技术动作**，例如 `① 落地原始文件\n写 /data/uploads/{filename}`——读者一眼能看到业务阶段（落地/扫描/归档）与对应高危动作（写文件/拼命令）。这样图本身就承担一部分业务说明，比纯技术节点更具可读性。
  - **图必须涵盖全部文件 / 命令 / 网络外呼 / 加解密操作**——**每个一节点**，标签含**动作 + 完整路径或命令行或 URL**（动态部分用 `{var}` 模板）+ `<br/><small>文件:行号</small>`。节点应包括：用户输入来源（`path.X` / `body.Y` / `query.Z` / `header.W`）、**关键控制点判断**（用 `{...}` 菱形）、文件 I/O / 命令 / 网络外呼 / 加解密（每项独立节点）。**与安全无关的通用控制流**（5xx 错误页、通用异常包装、内部纯计算路径）仍属噪声，不画。
  - **循环 / 分支用 flowchart 原生语法**：循环以 `subgraph "对每个 X 依次："` 包住循环体节点；分支用决策节点 `{...}` + 边标 `|是|`/`|否|`/具体条件；gate 校验失败画到"异常终止"subgraph。
  - **必画图**：流程含 ≥ 2 个文件/命令/网络外呼操作，或含循环 / 分支 / 关键控制点判断。**可不画图**：单一 I/O 且无判断（一次 SELECT 返回的极简读接口）——但仍需在文字里给完整路径。
  - **绘图约定**：
    - 用 `subgraph` 按阶段分组（例：`前置校验` / `核心处理` / `异常终止`），让阅读者一眼看清流程骨架
    - 判断节点用 `{...}` 菱形形状，边上用 `|是|` / `|否|` / 具体条件 标注
    - **每个节点附 `文件:行号`** 锚点（节点标签内用 `<br/><small>文件:行号</small>`），便于读者跳转代码
    - 必要时附 `style <node> fill:... stroke:...` 等视觉指令以提升可读性
  - 每接口至多一张图，仅画主干 + 关键分支。
- **编号步骤**：内在顺序的操作序列。
- **字段化短列表**：多类独立事实，书写为 `**第三方**: ...` `**加解密**: ...`，**仅列出实际发生的类别**。
- **连贯叙述**：事实可自然串接成句时。
- **一句话**：简单读接口。

递归追溯所得的下游事实写入同一条描述。**不使用嵌套子标题**。

**请求参数记号**（Body 字段树通用约定）：

- `string!` / `int!` —— `!` 表示非空/必填（来源于 `@NotNull`/`@NotBlank` 等）
- `string?` —— 可选/可为空
- `[ ... ]` —— 数组
- `// ...` —— 行内注释，标注约束（长度、正则、范围）或字段含义
- 末尾附 DTO 全限定名 + `文件:行号`

简单 query 使用一行 inline（`**请求**: query \`?page=1&size=20\` ...`）；嵌套或字段较多时使用 fenced 代码块（` ```json5 ... ``` `）。GET 请求无 body 时不写 body 行。

**落盘前自检**：subagent 写完 `endpoint-*.md` 后**必须做一次结构自检**，任一项不满足立即修补重写、再次自检通过才落盘——**绝不写入残缺产物**：

- 顶层 `# {METHOD} {URL} — ...` 标题存在
- 标题后有业务描述段落（≥ 1 段）
- 满足"必画图条件"（≥ 2 个文件/命令/网络外呼操作 或 含循环/分支/关键控制点判断）时，含 ` ```mermaid\nflowchart TD ... ``` ` 块且块内 ≥ 1 个文件/命令/外呼节点
- REST/RPC/MQ 接口含 `- **请求**:` 行
- 全文不含禁词（"风险/漏洞/不安全/缺乏校验/建议加固/可能被攻击"）

## 输出文件名约定

slug 小写连字符，中文转拼音/英文同义词；**默认覆盖同名文件**便于 git diff。

| 文件 | 写入者 | 用途 |
|---|---|---|
| `_plan.md` | `planner` | 子任务计划（步骤 2 产出） |
| `interfaces.md` | `interface-catalog` | 对外接口清单（攻击面索引） |
| `endpoint-{METHOD}-{path-slug}.md` | `endpoint-analyst` | 单接口分析（每接口一份） |
| `features.md` | `aggregator-writer` | 粒度 B 默认全量索引 |
| `features-{slug}.md` | `aggregator-writer` | 限定范围 / 大型项目子功能索引 |
| `overview.md` | `aggregator-writer` | 整项目顶层索引（粒度 A） |
| `_audit.md` | `completion-verifier` | 任务完整性审计报告 |

## 工作目录与模板

`docs/workspace-layout.md` 详述被分析项目内 `biz-flow-recon/{knowledge,templates,output}/` 的约定。**每次输出前必须读取对应模板后再行撰写**，不得依赖记忆产出——以确保用户对模板的修改能即时生效。
