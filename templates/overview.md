<!--
biz-flow-recon 输出模板（粒度 A：整项目）。
本文件可被被分析项目里的 `biz-flow-recon/templates/overview.md` 覆盖。
仅当用户明确说"整个项目""全量梳理""系统全貌"等总览意图时使用。

模板有两个分支：
  小项目（一次分析） → 用上半部分（核心总结 + 业务流详情 + 未跟到的引用）
  大项目（自动拆分） → 用下半部分（核心总结 + 子功能索引 + 未跟到的引用汇总），
                       详情已在各 features-{slug}.md 里
-->

# {项目名} 业务流讲解

## 核心功能总结

150-300 字连贯叙述：系统在做什么、给谁用、主要角色之间怎么协作、关键流程怎么串起来。要像同事 brief 一样成段叙述，不要 bullet。读完没看过代码的人也能复述系统在做什么。

---

## 分支 1：小项目（一次分析装得下）

## 业务流

### 子功能 1：...

#### {METHOD} {URL}

（接口下用 1-2 段散文，结构同 default.md。把入口、触发者、文件 I/O、外部命令、第三方交互、加解密自然织进段落。没发生的不写。下钻得到的下游事实继续织进同一段。）

### 子功能 2：...

...

## 未跟到的引用

仅当存在未在工作区找到的下钻目标时才写这一节，按 `<引用> — 调用点 (文件:行号)` 一条一行；没有就**整节略掉**。

---

## 分支 2：大项目（自动拆分）

省略上面的"业务流"详情节（详情已写在各 `features-{slug}.md` 里），改用下面两节：

## 子功能索引

- **认证与会话** — [features-auth.md](features-auth.md)：用户登录、会话保持、权限校验。
- **订单管理** — [features-order.md](features-order.md)：下单、修改、查询、取消。
- **支付** — [features-pay.md](features-pay.md)：支付下单、对账、退款。
- **用户中心** — [features-user.md](features-user.md)：用户资料、设置、消息。
- ...

每条 1-2 句话讲这个子功能在做什么，给读者一个跳转决策的依据。

## 未跟到的引用（汇总）

把所有 `features-*.md` 末尾的"未跟到的引用"合并到这里，去重后按 `<引用> — 调用点 (文件:行号)` 一条一行；没有就**整节略掉**。这样安全测试人员可以一次性看到所有盲点。
